server:
  port: 8080
  servlet:
    context-path:

spring:
  application:
    name: test-login-react
  datasource:
    url: jdbc:mysql://localhost:3306/car_rental
    username: root
    password: admin
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
  resources:
    cache:
      period: 0
  web:
    resources:
      cache:
        cache control:
          no-cache: true